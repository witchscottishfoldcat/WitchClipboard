# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [未发布]

### 变更

- 项目许可由 CC BY-NC-SA 4.0 更换为 [PolyForm Noncommercial 1.0.0](https://polyformproject.org/licenses/noncommercial/1.0.0)：仍仅限非商业使用，不再要求衍生作品相同方式共享；商业使用仍需联系作者取得额外授权。已发布的历史版本继续按其随附的旧许可执行。

## [1.5.1] - 2026-08-29

### 改进

- 修复主页面置顶/删除后界面不刷新的问题。
- 统一主页面与迷你页面的主题、边框和窗口定位表现。
- 导航栏标签最多选择 5 个，并兼容旧配置自动归一化。

## [1.5.0] - 2026-07-31

### 新增

- 搜索支持匹配来源程序，可用 `msedge`、`explorer` 等进程名快速定位记录。
- 顶部导航扩展模型、代码、颜色、路径、邮箱和数字分类，并可在设置中自由选择显示项。
- 新增 5 秒复制会话关联：同一组内容最多展示 5 条，可一键展开、收起和复制。
- 跨设备剪贴板支持电脑向手机发送图片；当前剪贴板自动同步，电脑选中哪条就发送哪条。
- 设置页新增七套按钮调色盘、开发者资料、网站、邮箱与开源许可信息。
- 新增 Windows ARM64、macOS Intel 与 Apple Silicon 实验性打包配置和自动发布工作流。

### 改进

- 项目名称与发布仓库统一为 Witch Clipboard / `witchscottishfoldcat/Witch-Clipboard`。
- 重绘应用和托盘图标，统一尺寸、留白、金色材质与剪贴板比例。
- 手机 Web 端改为中性毛玻璃设计，移除紫色品牌依赖。
- 调整列表顶部安全留白、关联区域折叠交互与全局按钮配色。

### 安全

- 跨设备传输仅在用户主动开启后于当前局域网运行，关闭后配对地址立即失效。
- Key、Token、密码与文件默认拒绝发送；历史数据库与图片继续保持本地加密。

### 发布

- 安装包文件名加入架构标识，区分 `x64` 与 `arm64`。
- 演示视频、旁白、字幕、录屏工程和导出文件全部加入 Git 忽略规则，不进入源码仓库。

## [1.2.0] - 2026-07-30

### 新增

- 新增「链接」与「Key」快速筛选，完整面板和迷你面板均可使用。
- 新增同一 Wi‑Fi 下的跨设备剪贴板体验版：电脑显示二维码，手机无需安装 App 即可配对。
- 支持电脑与手机双向发送文字和链接；手机发来的内容会写入电脑剪贴板并进入历史记录。
- 手机端改为中性毛玻璃界面；连接后立即同步电脑当前剪贴板，手机在线时可在电脑列表中点选即发送。
- 设置页可自定义顶部导航栏分类，额外提供代码、颜色、路径、邮箱和数字筛选。
- 设置页新增全局按钮调色盘，可在紫罗兰、海蓝、湖蓝、青绿、森绿、琥珀和玫红之间切换。
- 跨设备剪贴板支持电脑向手机发送图片，手机端可预览、打开或长按保存。
- 新增模型名称识别和“关联配置”：自动检索同一来源、15 分钟内连续复制的 Key、URL 与模型名称。

### 安全

- Key、Token、密码、图片和文件默认不参与跨设备同步。
- 每次启动生成随机配对地址，关闭跨设备连接后原二维码立即失效。

### 修复

- 给历史列表首行增加安全留白，避免选中圆角和边框贴住顶部时被滚动容器裁切。

## [1.1.0] - 2026-07-30

### 新增

- **全局快粘**：默认 `Ctrl+Alt+1…9` 可在任意程序中直接粘贴列表前九条，不再要求 WitchCat 面板保持焦点。
- 设置页新增「快粘热键」。用户可修改修饰键组合，数字键 `1…9` 固定；九个组合中任意一个被占用时，
  会保留原来的可用设置。

### 改进

- 唤出热键和快粘热键都采用安全重注册：新组合被占用时保留旧组合和旧设置，不再出现修改失败后
  原快捷键也失效的情况。

## [1.0.1] - 2026-07-27

### 修复

- **托盘「退出」点了没反应，应用只能上任务管理器杀。**
  常驻托盘的窗口都会拦截 `close`（点 × 只隐藏），退出时必须放行。完整面板判断了退出标志，
  但 1.0.0 新加的迷你面板是**无条件** `preventDefault()`——`app.quit()` 走到关窗口这一步
  就被它一直否决，整个退出流程卡死。
  现在退出标志由 `window.ts` 统一提供（`isQuitting()`），两个窗口都用它。
- 自检增加 4 条断言覆盖这条路径（退出前点关闭只隐藏、退出时两个窗口都放行）。
  撤掉修复后这条断言会红，确认它真的能抓住回归。

## [1.0.0] - 2026-07-26

加入自动更新。以 GitHub Releases 为源，但**一切都要用户点**。

### 新增

- **检查更新按钮**：设置页「关于与更新」里显示当前版本，点一下手动查。
  查完会如实告诉你结果：已是最新 / 有新版本 / 检查失败（附原因）/ 开发模式不支持。
- **更新流程全程用户说了算**：
  - `autoDownload = false`——查到新版本只提示，绝不先偷偷下 100 MB
  - `autoInstallOnAppQuit = false`——也不会在你关掉应用时悄悄装上
  - 下载要点「下载更新」，装要点「重启并安装」，中间有进度条
- **「暂不更新」**：点了就记住这个版本号（存进 `settings.json` 的 `skippedVersion`），
  之后启动时的自动检查不再提示同一个版本——每次开机弹一遍才是让人彻底关掉更新的原因。
  手动点「检查更新」仍然会如实报告，不会因为跳过就骗你说已是最新。
- 面板顶部一条**可关闭的提示条**：有新版本时显示「有新版本 vX.Y.Z」+ 查看 / ×（暂不）。
  只提示，不挡路。
- 启动后延迟 12 秒才自动查一次，不和采集、建库抢启动那几秒；查不到、出错都不打扰。

### 变更

- `electron-builder` 增加 `publish` 配置（GitHub），构建时生成 `app-update.yml` 与 `latest.yml`。
- 发布产物新增 `latest.yml`（更新检查靠它）与 `.blockmap`（支持差量下载）。

### 修复

- **更新检查在打包版里必然失败**：`electron-updater` 是 CJS，主进程打包成 CJS 之后
  动态 `import()` 拿到的命名空间会把导出包进 `default`，`mod.autoUpdater` 是 `undefined`，
  于是设置 `autoDownload` 时直接抛 `TypeError`。现在两种模块形状都认。
  这个 bug 只在**装好的版本**里出现，开发模式压根走不到那条路径。
- 更新过程写日志到 `%APPDATA%\WitchCat-Clipboard\update.log`：打包后的应用没有控制台，
  上面那个 bug 原来只 `console.error`，对用户和维护者都完全不可见。有了日志才定位到它。

### 已知限制

- 安装包**仍然没有代码签名**，更新时 Windows 会再弹一次 SmartScreen 提示。
- 更新走 GitHub Releases，网络不通时检查会失败——自动检查失败不打扰，手动检查会显示原因。

## [0.1.0] - 2026-07-26

首个发布版本。Windows 剪贴板管理器：托盘常驻、加密持久化、自动粘贴。

### 新增

**外观**

- **Logo**：剪贴板的「夹子」做成巫师帽、「板面」做成猫脸，一个形状同时读出剪贴板、猫、
  巫师三层意思。母版是 `resources/logo.svg`，另有托盘专用简化版 `logo-tray.svg`
  （去掉胡须/鼻子/帽带、帽子和眼睛放大一档，因为 16~32px 下细节只会糊成一团）。
- 图标由 SVG 母版经 Chromium 光栅化生成（`npm run icons`），设计源只有一份，
  不引 sharp/resvg 这类需要编译的依赖；产出 `tray@2x.png` 供高分屏自动挑选。
- 两个面板左上角的标识也用同一个 logo（内联 `resources/logo-tray.svg`，不在 `src` 下
  再放一份设计源），不再是通用剪贴板图标。

**采集与存储**

- 剪贴板自动监听：用 Win32 `GetClipboardSequenceNumber` 判断变化，400ms 轮询空闲时几乎不耗 CPU；
  拿不到原生能力时退化为内容指纹比对。
- 文字、图片、**文件**三种类型入库。文件只记录路径不复制内容，复制大文件（视频等）库里只多一行。
- SQLite + FTS5（`trigram` 分词器）全文检索，支持中文子串；1~2 字的短关键词回退到转义 `LIKE`。
- 图片内容寻址存储 `blobs/<前2位>/<sha256>.bin`，sha256 去重，库内只存缩略图，原图按需解密。
- 自动分类：链接 / 代码 / 颜色 / 路径 / 邮箱 / 数字。
- 记录来源进程名。

**界面**

- **迷你预览面板**（340×470，单击托盘弹出，贴着托盘图标）：文字看首行、图片看缩略图、
  文件看名字 + 大小 + 目录；直接打字即搜索，`Enter` / 双击粘贴，`Alt+1…9` 快贴。
- **完整面板**（820×540，`Alt+V`）：无边框 Win11 亚克力材质、虚拟滚动列表、类型与标签筛选、
  置顶、标签编辑、原图预览、条目详情、操作反馈 toast、空库引导。
- 全键盘操作；点到别处（点桌面 / 别的程序 / Alt+Tab）自动收起。
- 浅色 / 深色 / 跟随系统。

**粘贴**

- 记住唤出前的前台窗口 → 写剪贴板 → 隐藏面板 → `SetForegroundWindow` 还原 →
  释放残留修饰键 → 模拟 `Ctrl+V`。
- 文件条目写回真正的 `CF_HDROP`，粘出去是文件本身；原生写入失败退回写路径文本。
- 自动粘贴失败时重新亮出面板并提示手动 `Ctrl+V`，不静默失败。

**加密与清理**

- 主密钥 32 字节随机，用 Electron `safeStorage`（Windows DPAPI）保护后落盘。
- 数据库走 SQLCipher，图片走 AES-256-GCM，两者使用不同的派生子密钥。
- 敏感内容跳过：识别 `ExcludeClipboardContentFromMonitorProcessing` 等剪贴板标记，
  外加来源进程名黑名单（默认含常见密码管理器）。
- 保留策略：按条数 / 天数清理，置顶条目豁免；同时回收没人引用的图片文件。
- 设置页：外观、热键录制、行为开关、保留策略、数据目录、安全状态自检。

**工程**

- 数据目录固定为 `%APPDATA%\WitchCat-Clipboard`，不随「开发 / 打包」变化——Electron 默认的
  userData 路径来自应用名，开发取 `package.json` 的 `name`、打包取 `productName`，
  两者不一致就会读到两个不同的库。
- 从早期内部版本（曾叫 ZTB）的目录一次性迁移数据，**只搬不删**，失败只记日志；迁移会连带
  复制 profile 的 `Local State`——`safeStorage` 在 Windows 上的密钥存在那个文件里，
  换数据目录等于换了一把随机密钥，不带上它搬过去的 `master.key` 就解不开。
- 数据库无法解密时不静默丢数据：弹窗让用户决定，旧库改名为 `clipboard.db.locked-<时间戳>` 保留；
  实在建不了库就降级到内存存储，并在界面上标明「重启会丢」。
- `npm run selftest`：在真实 Electron 运行时里跑 57 项断言。
- 零依赖 PNG 图标生成器（手写 PNG 编码 + 3×3 超采样抗锯齿）。
- electron-builder NSIS 安装包，可选安装位置，卸载不删数据目录；支持开机自启（静默驻托盘）。

### 修复

- 虚拟列表在鼠标滚动时会被选中项拽回原位。
- `settings.json` 带 UTF-8 BOM 时 `JSON.parse` 抛异常并静默回退默认值。
- 托盘单击的竞态：点托盘时面板先因失焦收起、紧接着 `click` 又把它弹回来，导致永远收不掉面板。
- 「点到别处收起」只靠 `blur` 事件不可靠：从托盘唤出时窗口可能显示了却拿不到焦点，
  于是永远不触发 blur。改为主动抢前台 + 前台进程看门狗，并保证「从未拿到前台」时不会自行消失。
- 启动链路和自检的异常被 `void` 吞掉，表现为「托盘有图标但面板永远不出来 / 自检少打几行」。

### 已知限制

- 仅支持 Windows；非 Windows 平台原生能力整体降级。
- Win11 可能把托盘图标收进溢出区，需要在任务栏设置里固定出来。
- `Ctrl+,` 可能被中文输入法吞掉。
- 安装包约 102 MB（Electron 运行时占绝大部分）。
